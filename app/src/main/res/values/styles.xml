<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AperitifTextAppearance">
        <item name="android:textColor">@color/aperitifColor</item>
    </style>

    <style name="EntreeTextAppearance">
        <item name="android:textColor">@color/entreeColor</item>
    </style>

    <style name="DishTextAppearance">
        <item name="android:textColor">@color/dishColor</item>
    </style>

    <style name="DessertTextAppearance">
        <item name="android:textColor">@color/dessertColor</item>
    </style>

</resources>
